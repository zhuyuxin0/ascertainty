# Backend API base URL the frontend talks to.
# - Local dev: leave at default (uvicorn on :8000)
# - Vercel:    override per-environment to the public backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000
